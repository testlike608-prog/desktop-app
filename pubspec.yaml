name: test_station_controller
description: Industrial Test Station Controller — Flutter + Python Backend
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP + WebSocket
  http: ^1.2.0
  web_socket_channel: ^2.4.0

  # State management
  provider: ^6.1.2

  # Utilities
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
